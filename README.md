# Next-word-prediction-rnn
Next-Word Prediction using Recurrent Neural Networks (RNN). This project analyzes long-term dependencies in sequential text data and implements an RNN model using Python and TensorFlow. The model is trained on a text dataset to predict the next word in a sentence.
